const mongoose = require('mongoose');
const Schema   = mongoose.Schema;


const table_nameKankaSchema = new Schema({
shemaArr
});

/* şemayı dışarı aktaralım */
module.exports = mongoose.model('table_nameKanka', table_nameKankaSchema);
